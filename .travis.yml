language: python

python:
  - 3.6

install:
  - git clone https://github.com/luciferavada/zshell-python-venv.git
  - source zshell-python-venv/python.sh
  - pip install -r .requirements
  - source .venv-activate

script:
  - venv-tornado-test

after_success:
  - codecov --token "${CODECOV}" > /dev/null

notifications:

  slack:

    rooms:
      - sugarush:ZmgII1nbdMZf6FEhq7PgiuAG#travis-ci

    template:
      - "Commit (<%{compare_url}|%{commit}>) to %{repository}@%{branch} <%{build_url}|build> %{result}"
      - "%{author}: %{commit_message}"

    on_pull_requests: false
