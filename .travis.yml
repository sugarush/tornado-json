language: python
python:
  - 2.7
install:
  - pip install -r .requirements
  - source .venv-activate
script:
  - coverage run --source "${MODULE}" --module "tornado.testing" "test"
  - codecov --required
  - codecov --token "${CODECOV}"
